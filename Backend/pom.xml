<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.nexabank</groupId>
    <artifactId>nexabank</artifactId>
    <!-- 1.0.0-SNAPSHOT means this is currently in development -->
    <version>1.0.0-SNAPSHOT</version>
    <!-- IMPORTANT: 'pom' packaging means this project just holds configurations and modules, not actual code -->
    <packaging>pom</packaging>

    <name>nexabank</name>
    <description>NexaBank Microservices Root Project</description>

    <!-- Define the versions centrally here, so all child modules use the exact same versions safely -->
    <properties>
        <java.version>17</java.version>
        <spring-boot.version>3.5.13</spring-boot.version>
        <!-- Spring Cloud release trains handle versions for Eureka, Gateway, etc. -->
        <spring-cloud.version>2025.0.1</spring-cloud.version>
    </properties>

    <!-- We will add our microservice module paths here later -->
    <modules>
        <module>services/discovery-server</module>  
        <module>services/apigateway</module>
        <module>services/accountservice</module>
        <module>services/authservice</module>
        <module>services/transactionservice</module>
        <module>services/notificationservice</module>
    </modules>

    <!-- dependencyManagement acts like a blueprint for dependencies -->
    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot Blueprint -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Cloud Blueprint -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- This plugin ensures everything builds using Java 17 -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
